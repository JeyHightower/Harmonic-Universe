from app import create_app; from app.api.models.user import User; from app.extensions import db; app = create_app(); with app.app_context(): demo_user = User.query.filter_by(email=\"demo@example.com\").first(); if not demo_user: print(\"Creating demo user\"); demo_user = User(username=\"demo\", email=\"demo@example.com\"); demo_user.set_password(\"demo123\"); db.session.add(demo_user); db.session.commit(); print(\"Demo user created\"); else: print(\"Demo user already exists\");
