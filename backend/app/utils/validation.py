def validate_physics_object(data):
    """Validate physics object data."""
    # Implement validation logic for physics object
    if not isinstance(data, dict):
        raise ValueError("Invalid data format for physics object")
    # Add more validation rules as needed
    return True


def validate_physics_constraint(data):
    """Validate physics constraint data."""
    # Implement validation logic for physics constraint
    if not isinstance(data, dict):
        raise ValueError("Invalid data format for physics constraint")
    # Add more validation rules as needed
    return True
